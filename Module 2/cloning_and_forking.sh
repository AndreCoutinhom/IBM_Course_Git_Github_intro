# Creates a identical copy of the remote repository
git clone

# Gets the latest copy of the repo from origin.
git fetch

# Review the code to compare changes
git diff

# Update the main branch to origin
git push

# Both fetch and merge the changes into your local repo
git pull

